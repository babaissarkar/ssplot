package math.prim;

public class Test {

	public static void main(String[] args) {
		Complex nums[][] = new Complex[2][2];

		for (int i = 0; i < 2; i++) {
			for (int j = 0; j < 2; j++) {
				nums[i][j] = new Complex(i+1, i+1);
			}
		}

		GenMatrix<Complex> m = new GenMatrix<Complex> (nums, 2, 2);
		GenMatrix<Complex> m2 = new GenMatrix<Complex> (nums, 2, 2);

		System.out.println(m.toString() + "\n");
		System.out.println(m2.toString() +  "\n");
		System.out.println(m.add(m2).toString());

	}
}
